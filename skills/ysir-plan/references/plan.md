
## 引入第三方依赖/技术(可选)

- {第三方依赖名字}: {引入原因}

## 实现思路

> 注重代码质量(实际生成时不要包含此行)

- {要点1}
- {要点n}

## 预期改动范围

- {改动点1}
- {改动点n}

## 遗留风险点(可选)

> 考虑对已有功能或架构设计的影响(实际生成时不要包含此行)

## 分迭代的实现计划

> 迭代粒度必须与 git 提交对齐，每个迭代都是一个可独立提交的完整成果。(实际生成时不要包含此行)
> 每个迭代都应包含该提交所需的完整闭环工作；不得将开发、测试、验证等拆分到不同迭代。(实际生成时不要包含此行)

- {迭代1}

- {迭代n}

## 已完成列表

> 由开发者更新，计划阶段留空即可(实际生成时不要包含此行)

- {迭代1}: {补充说明(可选)}
- {迭代n}: {补充说明(可选)}
